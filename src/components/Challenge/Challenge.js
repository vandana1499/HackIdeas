import Layout from "../Layout/Layout";
import Container from "react-bootstrap/Container";
const Challenge = () => {
  return (
    <Layout>
      <Container>
        {/* <Form.Control id="filterCars" onChange={handleChange} />
        <CardGroup> */}
        {/* <Row xs={1} md={2} className="g-4">
            {filterData &&
              filterData.map((car, index) => (
                <VehicleItem key={index} car={car} />
              ))}
          </Row> */}
        <h1>Data</h1>
        {/* </CardGroup> */}
      </Container>
    </Layout>
  );
};
export default Challenge;
